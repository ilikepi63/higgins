[package]
name = "example_record_batch"
version = "0.1.0"
edition = "2024"


[dependencies]
arrow = { version = "55.1.0", features = ["ffi"] }
higgins-functions = { path = "../../", default-features = false }

[lib]
crate-type = ["cdylib"]
