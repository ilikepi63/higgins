[schema.amount]
id = "string"
data = "int32"

[streams.amount]
schema = "amount"
partition_key = ""

[streams.result]
base = "amount"
type = "reduce"
partition_key = ""
schema = "amount"
fn = "reduce"
