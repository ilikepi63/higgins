[storage.memory]
type="memory"

[schema.value]
id = "string"
some_data = "string"
other_data = "string"
i = "int32"

[streams.value]
schema = "value"
partition_key = "id"

[streams.value_windowed]
base = "value"
type = "window"
window = {
    type = "time" # or count
    interval = "5m" # or simply 5 if count
}
